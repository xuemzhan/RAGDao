数据清洗与预处理，好比在烹饪前“清洗和处理蔬菜”。其目标是消除原始数据中的“泥土和杂质”（噪音），并将其转化为一种标准化的、机器易于“消化”的格式。在RAG中，“噪音”不仅指无用的字符或格式，更包括任何可能误导嵌入模型进行语义理解的因素。嵌入模型会将文本映射到高维向量空间，文本中微小的、无意义的差异都可能导致向量位置的巨大变化，从而影响检索的准确性。

> 文本预处理（Text Preprocessing）是自然语言处理（NLP）中的一个基础且关键的步骤。其概念和方法在主流NLP教科书中有详细论述，如Dan Jurafsky和James H. Martin所著的《Speech and Language Processing》
> 

## **删除重复内容 (Deduplication)**

重复内容是知识库的“脂肪”，不仅增加了存储和计算成本，更严重的是它会“稀释”检索结果。当用户查询时，如果检索器返回了三份内容完全相同的文档，这不仅占用了宝贵的上下文窗口，还可能让LLM在生成答案时产生不必要的重复。重复分为两种：

- **完全重复：** 文件或文本内容一字不差。
- **近似/语义重复：** 内容高度相似，例如同一篇新闻稿的不同版本、同一产品的不同角度描述等。

### **方法与示例：**

- **完全重复检测：** 使用哈希算法（如MD5, SHA256）对文本内容计算哈希值。哈希值相同则视为完全重复。这种方法速度极快，适合初步筛选。
- **近似重复检测：**
    - **类比：** 这就像是给每篇文档生成一个独特的“语义指纹”。如果两枚指纹高度相似，我们就认为这两篇文档内容相近。
    - **方法：** 对文档或大块文本计算嵌入向量，然后通过计算向量间的余弦相似度来判断其内容的相似程度。设定一个阈值（例如0.98），超过该阈值则视为近似重复。对于海量数据，两两比较（O(n²)）成本过高，通常会采用 **MinHash + LSH (局部敏感哈希)**等技术先进行快速分组，再在小组内进行精确比较。
- **可执行代码示例 (近似重复检测):**

```python
# 准备环境:
# pip install sentence-transformers torch

from sentence_transformers import SentenceTransformer, util
import torch

# 1. 加载一个高效的预训练嵌入模型
# all-MiniLM-L6-v2 是一个在性能和速度上取得良好平衡的流行模型。
print("Loading embedding model...")
model = SentenceTransformer('all-MiniLM-L6-v2')
print("Model loaded.")

# 2. 示例文档列表 - 使用2025年热点话题
documents = [
    "ChatGPT推出了全新的AI助手功能，可以处理多模态输入。",  # 文档0
    "OpenAI发布ChatGPT最新版本，支持图像、音频和文本的综合处理。",  # 文档1 (与0语义重复)
    "今日北京天气晴朗，气温适宜。",  # 文档2
    "人工智能领域的投资在2025年创下新高。",  # 文档3 (主题相关但内容不同)
    "OpenAI的ChatGPT新增多模态功能，可同时理解文字和图像内容。",  # 文档4 (与0, 1语义重复)
    "特斯拉宣布其自动驾驶技术取得重大突破，FSD系统准确率提升至99.9%。",  # 文档5
    "Elon Musk表示特斯拉的完全自动驾驶功能已接近完美，识别准确率达到99.9%。",  # 文档6 (与5语义重复)
    "比特币价格突破12万美元大关，创历史新高。",  # 文档7
    "加密货币市场迎来牛市，BTC首次突破120,000美元。"  # 文档8 (与7语义重复)
]

# 3. 计算所有文档的嵌入向量
# .encode() 方法将文本列表转换为一个张量(tensor)，其中每一行是一个文档的向量表示。
print("Encoding documents...")
embeddings = model.encode(documents, convert_to_tensor=True)
print("Encoding complete. Shape of embeddings:", embeddings.shape)

# 4. 使用 util.cos_sim 计算所有向量对之间的余弦相似度
# 这会返回一个 N x N 的矩阵，其中 matrix[i][j] 是文档i和文档j的相似度。
cosine_scores = util.cos_sim(embeddings, embeddings)

# 5. 找出并标记近似重复的文档
threshold = 0.95  # 相似度阈值，可以根据需求调整
duplicate_indices = set() # 使用集合来存储待删除的重复文档索引，避免重复添加

print(f"\nFinding duplicates with threshold > {threshold}...")
# 我们遍历相似度矩阵的上半部分 (不包括对角线)
for i in range(len(documents)):
    for j in range(i + 1, len(documents)):
        # 如果两个不同文档的相似度超过阈值
        if cosine_scores[i][j] > threshold:
            print(f"Found duplicate: Doc {i} and Doc {j} (Score: {cosine_scores[i][j]:.4f})")
            # 将索引j的文档标记为重复项 (保留第一个出现的)
            duplicate_indices.add(j)

# 6. 创建一个去重后的文档列表
deduplicated_documents = [doc for i, doc in enumerate(documents) if i not in duplicate_indices]

print("\nOriginal Documents:")
for i, doc in enumerate(documents):
    print(f"{i}: {doc}")

print("\nDeduplicated Documents:")
for i, doc in enumerate(deduplicated_documents):
    print(f"- {doc}")
```

### **可能碰到的问题：**

- **阈值选择的艺术：** 近似重复的相似度阈值难以一概而论。太高，会漏掉很多语义重复的内容；太低，则可能将主题相关但内容不同的文档误判为重复。通常需要从一个较高的值（如0.98）开始，通过人工抽查被判为重复的样本来逐步降低和调整。

## **标准化文本格式 (Text Normalization)**

这是为了消除因格式问题导致的语义误解。对于嵌入模型来说，"RAG", "rag", "<p>R.A.G.</p>" 可能被编码成距离很远的向量，尽管它们指向同一个概念。

- **多语言环境的考量：**
    - **挑战：** 在处理包含多种语言的文档时，如果混合索引，可能会导致语言间的语义干扰。
    - **策略：** 在预处理阶段，应引入语言识别步骤（例如，使用langdetect等库）。识别出的语言信息应作为一个关键的**元数据**字段进行存储。在检索时，可以根据用户查询的语言，优先或仅在对应语言的文档块中进行搜索。
- **可执行代码示例 (综合处理函数):**

```python
# 准备环境:
# pip install beautifulsoup4

import re
from bs4 import BeautifulSoup
import unicodedata

def normalize_text(text: str) -> str:
    """一个全面的文本清洗和标准化函数"""
    
    # 1. 移除HTML标签
    # 使用BeautifulSoup库来解析和移除HTML标记
    soup = BeautifulSoup(text, "html.parser")
    text = soup.get_text(separator=" ") # 使用空格作为分隔符，避免单词粘连
    
    # 2. Unicode归一化
    # 'NFKC'形式会合并许多兼容字符，例如将全角字符转为半角，处理连字等。
    text = unicodedata.normalize('NFKC', text)
    
    # 3. 转换为小写
    text = text.lower()
    
    # 4. 移除URL和邮箱 (使用正则表达式)
    text = re.sub(r'https?://\S+|www\.\S+', '', text)
    text = re.sub(r'\S+@\S+', '', text)
    
    # 5. 移除特殊字符 (保留字母、数字、和基本标点)
    # 这个正则表达式会保留字母(a-z), 数字(0-9), 空格, 以及 . , ! ? -
    # text = re.sub(r'[^a-z0-9\s.,!?-]', '', text)
    
    # 6. 移除多余的空白字符
    # 将多个连续的空格、换行符、制表符替换为单个空格
    text = re.sub(r'\s+', ' ', text).strip()
    
    return text

# --- 示例文本 ---
raw_text = """
  <p>Hello World! 😊</p>
  Check our amazing site at https://example.com. 
  Contact us at help@example.com. 
  This is a full-width test：ＴＥＳＴ。
"""
clean_text = normalize_text(raw_text)

print("--- Raw Text ---")
print(raw_text)
print("\n--- Cleaned & Normalized Text ---")
print(clean_text)
# 输出: hello world! 😊 check our amazing site at . contact us at . this is a full-width test:test。
```

- **一个常见的陷阱——过度清洗：** 清洗规则必须根据领域知识来定制。例如，在处理代码知识库时，移除特殊字符（如 `*`, `{`, `}`）或转换大小写会完全破坏代码的含义。在处理金融或科学文献时，移除逗号或破折号也可能改变数字和公式的意义。

👉 **清洗前，请先思考你的数据类型**

## **词干提取 (Stemming) 与词形还原 (Lemmatization)**

### **类比：**

- **词干提取：**就像一个**粗暴的理发师**，用推子（规则）直接推掉头发（后缀），速度很快，但发型（结果）可能很难看（不是一个真实的单词）。例如，"studies", "studying" -> "studi"。
- **词形还原：**则像一位**专业的造型师**，他会查阅时尚杂志（字典）和考虑你的脸型（词性），为你设计一个最合适的原型发型（词元）。例如，"studies" (名词) -> "study", "studying" (动词) -> "study"。

### **在RAG中的抉择：**

- **强烈推荐使用词形还原，甚至不使用。** 现代的嵌入模型是在海量原始文本上训练的，它们本身就能很好地理解running和run之间的语义关系。对输入进行词干提取，反而会产生runn这样的非自然语言词汇，从而“污染”输入，导致嵌入质量下降。词形还原能保留词汇的自然性，对嵌入模型更友好，但也会增加处理时间。对于大多数现代RAG系统，**最常见的做法是只进行标准化，而不进行词形还原或词干提取**，完全信任嵌入模型的能力。
- **可执行代码示例 (使用spaCy进行词形还原):**

```python
# 准备环境:
# pip install spacy
# python -m spacy download en_core_web_sm

import spacy

# 加载spaCy的小型英语模型
# 模型会进行分词、词性标注等一系列NLP处理
print("Loading spaCy model...")
nlp = spacy.load("en_core_web_sm")
print("Model loaded.")

text = "The researchers are studying the effects of studying on students' performance."
doc = nlp(text)

# token.lemma_ 属性会返回该词的词元（基本形式）
lemmatized_tokens = [token.lemma_ for token in doc]

print("\n--- Original Text ---")
print(text)
print("\n--- Lemmatized Tokens ---")
print(lemmatized_tokens)
print("\n--- Lemmatized Text ---")
print(" ".join(lemmatized_tokens))
# 输出: the researcher be study the effect of study on student 's performance .
```